<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CriminalIntent" parent="Theme.Material3.Dark">
        <!-- Customize your dark theme here. -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowActionBar">@color/black</item>
        <item name="android:fontFamily">@font/montserrat</item>
        <item name="colorPrimary">@color/black</item>
        <item name="ic_menu">@drawable/ic_menu_add_light</item>
        <item name="ic_camera">@drawable/ic_camera_light</item>
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="ButtonStyle" parent="Widget.Material3.Button">
        <item name="backgroundTint">#F44336</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>